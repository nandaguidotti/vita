
  # AI Image Detection App

  This is a code bundle for AI Image Detection App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  